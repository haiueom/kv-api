{
    "name": "my-project",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "start": "wrangler dev",
        "format": "prettier --write **/*.{js,css,json,md}"
    },
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "itty-router": "^4.0.9",
        "wrangler": "^3.0.0"
    }
}
