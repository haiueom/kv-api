{
    "name": "my-project",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "start": "wrangler dev",
        "format": "prettier --write **/*.{js,css,json,md}"
    },
    "devDependencies": {
        "prettier": "^3.0.3"
    },
    "dependencies": {
        "itty-router": "^4.0.9",
        "wrangler": "^3.3.0"
    }
}
